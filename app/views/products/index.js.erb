$('#products').append('<tr><%= j(render @products) %></tr>'); // appends new products to what is already shown instead of replacing with *html*
$('.pagination').replaceWith('<%= j(paginate @products) %>');

