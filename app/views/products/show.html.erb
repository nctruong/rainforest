<h1>Product</h1>

<%= render 'product' %>

<h3>Reviews for <%= @product.name %></h3>

<%= render 'reviews' %>

<% if current_user %>
  <%= render 'review_form' %>
<% else %>
  <p>Please <%= link_to "log in", new_session_path %> to add a review.</p>
<% end %>

